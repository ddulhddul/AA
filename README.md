# 1장
- 비기능적 성능 변경용이성 깊이있게
- 병원으로 뺀다. // 

### 시스템의 동작/사업 환경에 대한 설명이 충분한가?
- 개발하고자하는시스템의 business driver는 무엇인지가 정의되어야 합니다.
- Business driver가 나오게 된 배경/환경에 대한 설명이 필요합니다.
- 서비스/기능의 필요성은 있는데, 동일한 서비스 중에서 경쟁우위가 될 만한 품질은 ?
- 이런기능이 되면, 이런기능이 어떻게되어야 남들보다 좋다 ! 이런게 나와야한다.

Business Drivers > Requirements > Architecture Drivers
//// Quality Requirements ////

차별화, 다른게 또 중요하다에 대한 고려

# 2장
2-1 구조에 영향을 미치는 기능 명세가 충분한가 ?
- Use Case 의사랑 환자가 어떻게 구분되는지 다시 구별하자. (로그인되서 한번에 뿌려지는것 말고)

2-2 UseCase의 관계가 명확한가?
- 외부 Actor 가 시스템을 어떻게 사용하는가를 명시하는것이다.
- stereotype 사용으로 구별을 명확하게 하는건 좋은데.... 

# 3장

# 4장
일정시간안에 얼마만큼 처리하느냐
양이 많아지는것에 대한 고민이 필요하다.

랜섬웨어, 디도스 공격 이런것들도 고민해야한다.
서버사이드의 구조를 결정하는 가장큰 요인중 하나다.

내가 설정한 business driver의 변경, business driver 내부에서의 변경

// 지금은 병원과 연동되지 않지만.
// 추후에 병원과 연동하여 그:쪽에서의 정보를 받는건 좋을듯.

// 나는 센서보다 서버쪽이니까 서버쪽으로 깊게 들어가서 생각해야대
// 표준은 뭐고 대안은 뭐다 이런거 고민


// 비기능적 요구사항은 제약 부분에 과제가 만족해야 할 측정값이 명시되어야 함.

// 암호화... 성능을 포기하고도 적용할만한가

// 



# Comments
- 개발하고자 하는 시스템의 business driver는 무엇인지가 정의되어야 합니다.
    - Business Drivers > Requirements > Architecture Drivers
- Business driver가 나오게 된 배경/환경에 대한 설명이 필요합니다.
- 시스템의 경계가 정의되어야 합니다.
- 시스템에 로그인한다가, 혈압 정보를 입력한다를 extend하는 use case일까요?
- Use case는 외부 사용자/actor의 시스템에 대한 쓰임새/사용 의도입니다. 불필요하게 내부 기능을 use case로 도출하는 것은 좋지 않습니다.
- 센서로부터 측정값이 입력되면, 저장/분석/처리하는 과정이 하나의 use case로 명세 되는 것이 좋습니다.
- 비가용시간인가요?
- 어떻게 테스트 할 것인지가 명확해야 합니다.
- 나중에 환자의 시스템과 의사의 시스템이 달라져야겠네요.
- 사용자가 많아지면서 발생하는 성능에 대해서 다뤄야 합니다. 그리고, scalability도 중요합니다.
- 시스템의 기능에 대한 변경에 대한 검토가 필요합니다. 특히 business driver로부터.
- 다른 질병의 원격진료에 적용하는 것은 어떨까요?
- 중요도/복잡도 분석이 품질 요구사항의 근거를 제공하고는 있지만, 품질 요구사항으로 선정된 사유/선정되지 않은 사유에 대해서 설명이 필요합니다.




### business driver ???
- 다른 서비스보다 철저한 보안성.
    - 개인정보를 저장하는 시스템이고, 환자의 개인정보가 많이 들어가기 때문.
    - 의사가 처방한 처방전이 악용될 위험이 있기 때문에, 병원에서 우리 시스템을 차용할때, 단 한번의 출력을 허용하고, 복제를 불가능하도록 하는 시스템으로 만들어야 한다. 
    - 사용자 개인정보를 DB에 저장시, 암호화 하는것을 고려해보자.
- 신뢰성.
    - 데이터가 전달되고 확인되는 과정이 어플리케이션으로 진행되다 보니, 의사에게 잘 전달이 되었는지, 처방받은 정보로 처방전을 뽑을 수 있는지
- 병원 시스템과의 연동 가능성 고려
    - 어플리케이션에서의 환자 정보 관리를 반대하는 병원이 있을 수 있다. 병원이 제공하는 정보만을 보여주는 시스템으로 변모 가능성 고려.
- 확인하지 않은 내용이 있다면 주기적 alert
    - 처방이나 환자 혈압정보 update내용을 놓치지 않도록 설계

- 서비스 비용을 줄여보자
    - 파일 압축 기술 적용
    - 서버의 클라우드화 ?
- 성능을 올려보자
    - 이미지 파일 전송

# 1. 고혈압 또는 저혈압 환자 관리 시스템
(2. 병원 진료기록 조회 서비스
병원에서 전송할 것과 환자가 봐야할 것)

고혈압 또는 저혈압 환자의 혈압 상태를 관리해주는 프로그램을 개발한다.
환자는 주기적인 혈압측정을 실시하여 데이터를 서버에 저장하고, 
주치의가 서버에 접속하며 데이터를 확인한 후, 
환자의 건강상태에 따라, 처방전을 서버를 통해 발급한다.
(처방전을 모바일을 통해 발급 할 수 있다고 가정.)

- 환자가 입력하는 시스템
    직접 입력 또는 기기를 통한 입력
    (Bluetooth, Wifi, NFC)
- 의사가 입력하는 시스템
    - 직접 입력후 시스템에서 처방전을 생성해주는 ? (보안문제검토)
    - 직인이 들어간 Pdf 또는 Jpeg 파일을 업로드 후, 환자가 활용 (출력 후 활용 ?)
- 입력 내용을 Notify (to 환자)
- 입력 내용을 Notify (to 의사)



## 블루투스 데이터 받아오는 예제
### [http://susemi99.kr/769](http://susemi99.kr/769)
그 모듈이 사용하는 프로토콜이 뭔지 부터 알아내세요.
AD2P, SPP 뭐 이런 것들 많아요.
spp 예제는 http://susemi99.kr/769 에 있습니다
 쎄미 (144,870 포인트) 님이 2014년 3월 13일 답변



# 할것 ??
- ppt 에 시스템 구조 작성하기
- 요구사항 명세서 작성 (Table)



1. 시스템 개요
```
    // 활동1. 시스템 정의
    // 점검1-1. 시스템의 경계(정의)가 명확한가?
    // 점검1-2. 시스템의 동작/사업 환경에 대한 설명이 충분한가?
```
2. 요구사항
```
    2.1. 기능적 요구사항
    // 활동2. 기능 명세
    // 점검2-1. 구조에 영향을 미치는 기능 명세가 충분한가?
    // 점검2-2. Use Case의 관계가 명확한가? (include/extend 등)
    // 점검2-3. 기능의 구분이 명시적인가? (stereotype 사용)
```
- 2.2. 비기능적 요구사항
```
    // 활동5. 품질 속성 선정
    // 점검5-2. 품질 요구사항의 명세가 적절한가?
    // 점검5-3. 품질 요구사항의 측정이 가능한가?
    // 점검5-4. 비기능적 요구사항의 제약이 명확한가?

    신뢰성 - 외부 Application(블루투스기기) 오류에 대한 영향
    가용성 - 시스템 가용성
    보안성 - 시스템 정보의 유출
    신뢰성 - 블루투스 기기와의 정확한 데이터 전달 및 분석
```

- 2.3. 품질 속성
```
    // 활동5. 품질 속성 선정
    // 점검5-2. 품질 요구사항의 명세가 적절한가?
    // 점검5-3. 품질 요구사항의 측정이 가능한가?

    성능 - 블루투스 기기의 연결 시간
    성능 - 블루투스 기기의 응답 시간(혈압 데이터, 프린터 출력)
    변경 용이성 - 기기의 연결 방식 변경(전자 혈압기, 프린터)

    성능 - File Storage(병원 직인) 저장 효율
    성능 - 시스템 정보의 조회 시간(사용자 정보, 처방전)
    성능 - 시스템 정보의 등록 시간(사용자 정보, 처방전)
    
    가용성 - 사용자 증가에 따른 시스템 대응

    변경 용이성 - 병원 Data Interface 에 따른 변경 Component 수
    변경 용이성 - 혈압계 말고(ex. 혈당계) 측정수단의 변경에 따른 Component 수

    가용성 - 시스템 복원 시간
```



### business driver ???
- 다른 서비스보다 철저한 보안성.
    - 개인정보를 저장하는 시스템이고, 환자의 개인정보가 많이 들어가기 때문.
    - 의사가 처방한 처방전이 악용될 위험이 있기 때문에, 병원에서 우리 시스템을 차용할때, 단 한번의 출력을 허용하고, 복제를 불가능하도록 하는 시스템으로 만들어야 한다. 
    - 사용자 개인정보를 DB에 저장시, 암호화 하는것을 고려해보자.
- 신뢰성.
    - 데이터가 전달되고 확인되는 과정이 어플리케이션으로 진행되다 보니, 의사에게 잘 전달이 되었는지, 처방받은 정보로 처방전을 뽑을 수 있는지
- 병원 시스템과의 연동 가능성 고려
    - 어플리케이션에서의 환자 정보 관리를 반대하는 병원이 있을 수 있다. 병원이 제공하는 정보만을 보여주는 시스템으로 변모 가능성 고려.
- 확인하지 않은 내용이 있다면 주기적 alert
    - 처방이나 환자 혈압정보 update내용을 놓치지 않도록 설계


- A. 도메인 모델
```
    // 활동3. 도메인 모델 정립하기
    // 점검3-1. 도메인 모델이 충분히 세분화 되었는가?
    // 점검3-2. 도메인 모델에 구조가 반영되지 않았는가?
```
- B. 품질 시나리오
```
    // 활동4. 품질 시나리오 생성
    // 점검4-1. 구조에 영향을 미치는 품질에 대한 검토가 충분한가?
    // 점검4-2. 성능에 대한 검토가 충분한가?
    // 점검4-3. 변경용이성/확장성에 대한 검토가 충분한가?
```



## MVC 의 장점
- View와 Model간의 간섭을 피하고 Controller가 중간 관리를 하는 역할을 하여 간접소통을 통해 좀더 유연한 구조를 설계할 수 있는것이 MVC의 가장 큰 장점입니다.
– 3가지 패턴 중 구현에 있어서 가장 심플
출처: http://blog.embian.com/49 [Embian Blog]

## MVC 의 단점
- 복잡한 구조로 보일 수있다.
- Model과 View의 완벽한 분리가 어렵다.
> 기본기능 설계를 위해 클래스들이 많이 필요하기 때문에 복잡할 수 있습니다. 이것은 속도가 중요한 프로그램에서는 권장 되지 않을 수 있습니다. 또한 설계시간이 오래 걸리고 숙련된 개발자가 필요합니다. 
가장 큰 문제점은 Model과 View의 의존성이 완벽히 분리 할 수 없기 때문에 패턴이 모호해질 수 있고 변형이 올 수 있습니다.
출처: http://blog.embian.com/49 [Embian Blog]
단점
– View와 Model 간의 의존성을 완전히 없앨 수 없음
– 프로그램이 커질수록 가독성이 떨어지고 유지보수가 힘들어짐




- 후보구조 15. Device to Device 로 환자와 의사간의 Communication
- 후보구조 16. Server Client 를 활용한 communication // Restful API
- 후보구조 19. Web Page 로 View 부터 서버까지 전부를 제공할 것인가 ? // MVC
            - DB 에 접근하는 웹서버를 따로 두고, Application 및 Client 서버를 따로 두어, Sensitive한 데이터관리와 아닌것의 관리를 나눌 것인가 ?
- 후보구조 18. 병원 직인 Img 파일을 사용자 개인의 Device 에 저장 Vs 서버에 저장후 매번 불러오기.
    - 후보구조 6. 모바일 기기로 서비스 할 경우, 이미지를 복사하여 무단 도용할 가능성이 있으므로, 이미지 파일 자체를 암호화 처리해서 로드할때 복호화 하는 기술 적용 (Component 적용)
        - http://mbsmin.tistory.com/entry/%EC%9D%B4%EB%AF%B8%EC%A7%80-%EC%95%94%ED%98%B8%ED%99%94%EC%B2%98%EB%A6%AC
        - 암호화 기술의 위치 (서버 or 클라이언트)
        - File Storage 를 외부 FTP 에 따로 두어 jpeg 파일을 컨트롤 하는 구조를 생각.
        - File Storage 를 서버 내부에 두어 경로만 DB 에 저장.
    - 후보구조 5. 직인 jpeg 파일 저장시, 압축 후 저장해서 시스템 storage 효율 향상
        - 압축 Controller 의 위치 서버 or Client
        - 이미지 resizing
            이미지의 크기를 고정하려고 한다면, 이미지 resize를 해야한다. 이미지의 크기가 줄어들면서 자연히 이미지 파일의 크기도 줄어든다. 주로, thumnail을 만들 때, 이미지 resize를 고려하는 것 같다.
            pobi's tip : 일반적으로 각각의 기기에 적합한 이미지 사이즈를 정하고, 원본이미지를 여러 개의 resied image로 저장해둔다. 압축을하지않더라도 resize만으로 파일크기가 줄어들기 때문에 가장 일반적으로 사용하는 방법이다.
            java image resize high quality / java image resize without loss quality 등으로 검색하면 된다.
            방법은 매우 여러가지이다. (보통은 어떤 라이브러리를 선택해 해결한다.)
                - ImageIO (java에서 제공하는 기본 라이브러리)
                - JAI (Java Advanced Imaging) 라이브러리 [JAI활용 예제코드] (http://blog.naver.com/PostView.nhn?blogId=mirmir96&logNo=70085487223)
                - Thumnailator (https://github.com/coobird/thumbnailator)
                - Image caling 오픈소스 (https://github.com/mortennobel/java-image-scaling)
    - 후보구조 8. 병원 직인 Storage.
        > 처음 생각은 처방전 Word 파일을 의사가 업로드하여 전송하여 서비스 하는 거였다면, 
        그와 반대로, 병원 직인에 해당하는 이미지만 서버가 가지고 있는 상태에서, 사용자에게 제공한다.
        더 나아가, 사용자는 대부분 갔던 병원에서만 처방전을 받으므로, 직인에 해당하는 이미지 파일을 
        서버에서 지속적으로 요청하여 내려받지 않아도 될 수 있도록, 직인에 대한 이미지 파일을 클라이언트의 기기에 암호화한 채 저장하여, 이미지 전송에 대한 비용적 부분을 최소로 하려고한다.
        
        - 이에대한 trade off 로, 기기 storage 에 대한 용량문제와 보안적인 측면이 부각된다.
        - 이를 해결하기위한 구조는 ... ?
    - 후보구조 N. 워드 파일을 저장하는 것이 아닌, 정보를 받아 시스템에서 자체적으로 문서를 생성하여 프린트 하는 구조를 취하자.
        - 저장공간 효율을 높이기 위해.

        
- 후보구조 3. 사용자 요청에 대한 Message Queue 적용.
    - 후보구조 13. 사용자증가에 따른 load balancing 고려, Dispatcher 모듈을 따로 만든다.
    - Message Queue 로 데이터를 입력받아 대기하여 처리하는 것과,
    - 서버의 갯수를 늘려 Dispatcher 를 놓고 가용한 서버를 지속 호출하는것.
    - 두가지는 Trade off 가 아니므로, 둘다 차용을 고려한다. 
- 후보구조 1. DB 암호화 적용
    - 따로 필요 없을듯하다... ?
- 후보구조 2. 타 병원과의 Interface 고려, Proxy 모듈(DB Access Interface) 적용
    - 프록시 패턴의 장점
        - 사이즈가 큰 객체가 로딩되기 전에도 proxy를 통해 참조를 할수 있다.(이미지,동영상등의 로딩에서 반응성 혹은 성능 향상 - 가상 프록시)
        - 실제 객체의 public, protected 메소드들을 숨기고 인터페이스를 통해 노출시킬수 있다. (안전성 - 보호 프록시)
        - 로컬에 있지 않고 떨어져 있는 객체를 사용할수 있다. (RMI, EJB 의 분산처리등 -  원격 프록시)
        - 원래 객체의 접근에 대해서 사전처리를 할수 있다 (객체의 레퍼런스 카운트 처리 - 스마트 프록시)
        - 원본 객체의 참조만 필요할때는 원복 객체를 사용하다가, 최대한 늦게 복사가 필요한 시점에 원본 객체를 복사하여 사용하는 방식. (Concurrent package의 CopyInWriteArrayList - 변형된 가상 프록시)

    - 프록시 패턴의 단점
        - 객체를 생성할때 한단계를 거치게 되므로, 빈번한 객체 생성이 필요한 경우 성능이 저하될수 있다.
        - 프록시안에서 실제 객체 생성을 위해서 thread가 생성되고 동기화가 구현되야 하는 경우 성능이 저하되고 로직이 난해해질 수 있다
    - 프록시 패턴을 사용하지 않는다면 ? 
        - Proxy 를 사용하지않고 직접적으로 접근한다면.... 이미지 전송시에 시간적인 측면.
        - 현재의 프로세스상에서는 나오지 않은, 다른 병원과의 확장을 고려할 때, 내부 component 의 변경이 크게 일어나게 될 수 밖에 없다.
- 후보구조 3. Watch Dog 프로세스
    - 기기 내의 Watch Dog 프로세스를두어, 프린터 출력 명령 및 서버로의 데이터 전송시, 에러가 없도록.
    - Watch dog 은 새로운 Thread 로 돌린다.
- 후보구조 11. 프린트시, 오류가 발생한다면 ? 
    - (통신오류 또는 블루투스 오류 또는 프린트 기기 내부 적인 오류로 인한 프린트 불가능 이상현상 발생)
    - 프린트를 1회만 할수 있기 때문에 이에 대한 내용은 반드시 신뢰성이 고려되어야 한다.
    - 프린트 신뢰성을 위한 Watch Dog 추가 운용
- 후보구조 7. 캡처 불가 기능 적용. (적용하지 않음)
    - 미리보기시, 화질이 안좋은 형태고 제공후, 프린터 모듈을 통해서만 좋은 화질로의 프린트가 가능하도록 한다.

- 후보구조 9. 처방전 프린트는 1회만 가능하도록 한다. 

- 후보구조 12. DB 용량 증가에 따른 Master-Slave 구조 채택을 위한... ? 

- 후보구조 14. 처방전 결재 관련 모듈 ?? 
    - 추후 결재관련 서비스가 추가될 요인이 많으므로, 이를 고려, 처방전이 

- 후보구조 17. 약사에게도 UI 제공으로, 환자의 처방전을 프린트하지 않고도 서비스 할 수 있는 기술
- 후보구조 20. FireWall 도입 외부로 호출할때


~~구조로 하면 ~~가 별론데
~~가 좋은 구조는 뭐지 ?





보안
- 서버 내 데이터의 외부  반출 => 보안 정책에 따라 대응
- DDOS 공격 방어 => DNS 서버 변경으로 대응
- Client 로부터 개인정보 수집 => 고지문 고지,동의
- 개인정보 저장 처리 => DB 암호화
- 통신구간 보호 => 
- 서버 웹 포탈 보안 => 악성파일 업로드, SQL Injection   체크, CSRF 체크, 중요파일 다운로드, 파라미터 변조, 
- Admin 페이지도 동일 보안수준 
- 로깅 서버 => 개인정보 조회시 감사를 위한 로깅 서버 구축
- 환자의 오픈할 의사별 데이터 선택 -> 오픈할 의사와 데이터 선택함.
- Client 로부터 전달되는 데이터를 믿을 수 있는가?


완벽한 의사결정은 없다.

성능 1,2 개 변경용이성 1,2개

// 컴퓨터, 프로세스, 스레드 단계로 deployment 를 그려라
// CNC(Component & Connector) 는 각각 항목에서 생각해보는거로
// 3장에 시스템에동작에 관련된 설명
// module view, Module relationship 는 4장에 // Component(Module) spec과 구현

// F는 큰그림만. (Deployment)
// Risk에대한 검토.... !!






### 2차 리뷰
- 얘기를 할때 무슨 내용인지잘 확인하고...
- 어떤어떤걸 썻다 이랬을때 그걸 왜썻냐 ? 이런거

성능
보안성
가용성
변경용이성

- 서버단에 집중해서하도록...
- 어떤 문제가 진짜 있을지 핵심을 다루라...
- 시스템 초기의 구조적 의사결정...
    - 전체적인 개발에 영향을많이 주는 것
    - 최종 품질에 영향을 많이 미친다.


#### 구조설계 프로세스 ?
- 어떤 기능이 필요하다 까지만 전까지 햇지만, 이번엔 품질을 잘 만족하기 위해서 도메인 모델을 어떻게 작성할것인가 구조적인 의사결정을 하는것.
- 후보 구조 설계.
- 창의적인 방법과 경험이 필요하다...
- 상충되는 후보구조를 비교분석 햏서 선정하거나 다 버려질수도 잇고 다 선정될수도잇고
- architecture style들을 쭉 검토해보는게 좋다.
- 구조적의사결정을 잘 만족할 수 있는 전체적인 무엇인가.... architecture style
- 3,4 장에서는 장점이 많이 드러나도록 표현해야하징

##### 6-2 성능에 대한 분석과 후보 구조가 적절한가 ?
- 센서로부터의 정보 처리
    - 센서로부터의 입력을 조정하는 방법
    - 처리에 필요한 리소스를 관리하는 방법
        - 가용성 측면과 성능 측면을 동시에 비교
    - 분석을 서버에서할 것이냐 디바이스에서 할 것이냐 ...
    - 분석한것만 저장할거냐 전체를 저장할거냐
- 의료 정보의 열람
    - 데이터를 조회할때... Pre-fetch / Cache / 중복저장 ?
- 의료 정보의 저장
    - 유지보수의 용이성 (단말 vs 서버) // 서버가 용이
    - 단말에 두는게 보안성이 더 좋다 ?
- database
    - cloud native에 대한 고려... 구글링을 통해 찾아보는게 좋은 방법이다.
    - relational database vs NoSQL
    - time series database
    - in-memory database
    - database replication
    - database shard
    - 많은 의료정보데이터를 cashing 하는 database 를 써서, 자동 배치 되도록 하면 효율이 높다.
    - Database replication
        - master-slave replication
        - multi-master replication
    - database shard (데이터 나눠서 저장) 

> 장인은 detail에서 결정된다....

##### 6-3 변경용이성
- 센서의 추가 == 추상화 / plugin
    - UI 도 당연히 변경되잖아....
    - UI를 어떻게 다룰지.... 레포트 디바이스 리스트를 전체를 불러와서 쭊쭊 하는거
    - plugin vs micro kernel 다르다...

#### 6-1
- 보안 : 공격 시나리오가 구체적이어야 한다.
    - 공격이 어디로, 어디를 암호화하고, 어떤 암호화를 적용해야할지...... 암호화는 성능이 뚝뚝 떨어지니까...
- 가용성
    - fault를 어떻게 detect 하느냐.
    - 네트워크를 어떻게 가용을 유지하느냐 정확하게
- 확장성
    - Service Choreography (안무) 가 트렌드다...
    - process와 thread 의 차이... ?




### unit 2-5-1 Handling Modifiability
#### 고려사항
1. 무엇을 바꾸고자 하는가 ?
2. 가능성은 ? 중요성은 ?
3. 비용은 ? 난이도는 ?
    - 비용 = 변경하는 비용 x N개의 유사한 수정
4. 변경 메커니즘 ? 혜택 ? 
    변경 메커니즘의 혜택
    = (N X 매커니즘 없이 변경하는 비용) - 매커니즘을 설치하는 비용 - (N x 매커니즘을 사용하여 변경하는 비용)
    = N x (매커니즘 없이 변경하는 비용 - 매커니즘을 사용하여 변경하는 비용) - 매커니즘을 설치하는 비용

#### 기본원칙
- 모듈 <- 관리 단위
    - 모듈의 크기를 작도록 해야 관리가 용이하다.
    - 작게 자르는게 기본 원칙
- 변경 비용은 모듈의 크기에 비례한다.
- 응집도가 높을수록, 결합도가 나증ㄹ 수록 좋다.
- 변경요인을 Localization (하나로 집중)
- 기능의 추가
    - 추가가 시스템에 영향을 미친다면... ?
    - 그 부분을 seperation 해서 관리
- 변경이 다른 컴포넌트에 영향을 주는 경우
    - 연결 방식의 관리
    - 인터페이스의 관리 // DIP
    - 내가 관리하고자 하는게 무엇이냐가 중요.
- Tactic1. 모듈 나누기
    - 응집도를 깨트리지 않느 범위에서
- Tactic2. 공통 모듈 나누기
    - 공통(중복되는) 모듈을 나눈다.
- DIP ./// 의존성 
    - high level modules shouldnot depend on low level modules. both should depend on abstractions
    - abstractions should not depend on details. details should depend on abstractions.
- Tactic3. 의미로 묶기
    - 연관된 모듈/컴포넌트를 묶는다.
    - 연결을 제한한다.
    - tier 로 묶으면, 앞에 dispatcher 를 담당하는 놈을 명시해줘야해
- Tactic4. 느슨하게 연결하기
    - 모듈간의 연결을 느슨하게 하여, 변경이 상호 영향을 주지 않도록 한다.
    - publish/subscribe
    - pipe/filter
- Tactic5. 연결을 늦추기
    - runtime 시점에
- tactic6. 인터페이스 구분하기
    - 구현과 인터페이스를 구분한다.
    - 구현의 변경이 인터페이스에 영향이 미치지 않게 한다.
    - OCP //구현의 내부의 변경이 외부에 영향을 주지 않아야 한다.. 이 패키지 사용하는 외부에서 쉽게 확장할 수 있어야 한다.

- 구조에 가장 영향을 많이 미친 품질요소
- 대안은 없나
- 대안에 대해 적용을 고민해봣나
- 다른 대안은 없나

- pureMVC Model
- 안드로이드는 activity 단위 activity 여러개고, 이를관리하는 controller 로 표현 ?? 밑에 Shared repository
- 성능보다는 변경용이성...

#### 성능
1. 무엇을 해야하는가 ?
    1) 처리시간
    2) 통신시간
    3) 대기시간 // 중요 !
        - 작업대기
        - 리소스대기(cpu,메모리,통신)
2. 중요성은 ?
    [최소의미시간, 최대허용시간]
- 불필요한 것을 최소화한다.



3장은 시스템이 어떻게 동작하는지 이해할 수 있도록,장점과
Deployment / C&C view
4장은 어떻게 개발 할 수 있느냐가 목적이다.
개발에 충분한 요인들이 잘 설명되어 있느냐
work assignment
상세 스펙

### 질문 list
1. Application 의 경우, Activity 단위인데, UI로 정의한 boundary 를 하나의 Activity 로 설명해도 될런지 ?
    - Activity 를 하나의 instance 로
    - instance 들의 공통된 무언가를 뽑는게, Module view 로 볼 수 있다. 공통된 무언가를 뽑아서 그린것들을 해당 부서들에 work assignment 하라.

2. 3,4 그려야 하는것
3. 병원 직인에 대한 파일 암호화해서 서비스하는걸 중요하게 생각하는데, 스토리가....

= DBMS 오라클 DB2 이런거 적용 고민 하지 마라.
세부적인 솔루션 고민은안해도 된다.


